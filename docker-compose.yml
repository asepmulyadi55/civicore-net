services:
  api:
    platform: linux/amd64
    build:
      context: .
      dockerfile: CiviCore.Api/Dockerfile
    ports:
      - "5000:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - FrontendUrl=https://dwipapuri.amsite.click
      - ConnectionStrings__Redis=redis:6379
      # Encrypts Nomor KK at rest. `:?` makes compose REFUSE TO START when it is unset,
      # rather than letting the app fall back to the public key compiled into the source.
      # Set ENCRYPTION_KEY in .env — see docs/encryption-key-migration.md
      - Encryption__Key=${ENCRYPTION_KEY:?ENCRYPTION_KEY is not set. See docs/encryption-key-migration.md}
      # Only used when rotating keys later; blank means "the old built-in fallback".
      - Encryption__OldKey=${ENCRYPTION_OLD_KEY:-}
    volumes:
      - ./CiviCore.Api/wwwroot/public-media:/app/wwwroot/public-media
      - ./CiviCore.Api/App_Data/PrivateMedia:/app/App_Data/PrivateMedia
      # Serilog's rolling files, readable on the host at ./logs
      - ./logs:/app/logs
    # `docker compose logs` reads this. The json-file driver is UNBOUNDED by
    # default and will eventually fill the disk — cap it.
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "5"

  redis:
    image: redis:alpine
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  web:
    build:
      context: ./CiviCore.Web
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - API_INTERNAL_URL=http://api:8080
      - NEXT_PUBLIC_SITE_URL=https://dwipapuri.amsite.click

  frontend:
    build:
      context: ./CiviCore.Frontend
      dockerfile: Dockerfile
    ports:
      - "8080:8080"

volumes:
  redis_data:
